server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    # /<woche> auf die App mappen; echte Dateien (kw.js) gehen weiter direkt raus
    location / {
        try_files $uri /index.html;
    }
}
